# 相依套件（首次執行由 npm install 安裝）
node_modules/

# 產出（含真實個案截圖與資料，不進版控）
output/

# Open Data 快取（ZIP + CSV，檔案大，不進版控）
cache/

# 真實個案設定檔（個資，不上傳；請自行保存於本機）
案件設定_*.json

# Playwright / 暫存
.playwright-cli/
__pycache__/
*.pyc

# MCP client 設定（含本機絕對路徑，不入版控）
.mcp.json

# 系統
.DS_Store
Thumbs.db
